# assets/

Reserved for static assets used by the UI — e.g. a custom favicon or
logo passed to `st.set_page_config(page_icon=...)`, or screenshots
referenced from `README.md`. Empty for now; nothing in `app.py` reads
from this folder yet.
